# Projet Credit Scoring — API FastAPI

## Lancer en local (Windows)
```bat
cd "C:\Users\Maintenant Prêt\Desktop\Projet_credit_scoring"
.venv\Scripts\activate.bat
pip install -r requirements.txt
uvicorn api:app --reload